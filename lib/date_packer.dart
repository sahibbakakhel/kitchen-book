import 'package:flutter/material.dart';

class DatePacker extends StatefulWidget {
  const DatePacker({super.key});

  @override
  State<DatePacker> createState() => _DrawerState();
}

class _DrawerState extends State<DatePacker> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(
        child: Column(
          children: [

          ],
        ),
      ),
    );
  }
}